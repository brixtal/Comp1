#include <stdio.h>
#include <string.h>
#define MAX 50

int main (void)
{

	int matriz[MAX][MAX];

	scanf("%d",&k);
	scanf("%d",&l);

	for(i=0; i<k, i++)
	{
		for(j=0, j<l; j++)
		{
			scanf("%d",&matriz[i][j];
			printf("%d", matriz[i][j];
		}
		printf("\n");
	}

	return 0;
}